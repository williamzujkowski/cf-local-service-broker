apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-broker
  labels:
    app: postgres-broker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-broker
  template:
    metadata:
      labels:
        app: postgres-broker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres-broker
          image: postgres-broker:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: PORT
              value: "8080"
            - name: PG_HOST
              value: "postgresql.default.svc.cluster.local"
            - name: PG_PORT
              value: "5432"
            - name: PG_ADMIN_USER
              value: "postgres"
          envFrom:
            - secretRef:
                name: postgres-broker-creds
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-broker
  labels:
    app: postgres-broker
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: postgres-broker
